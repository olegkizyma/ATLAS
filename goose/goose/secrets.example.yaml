# Example secrets file for local development (do NOT commit real secrets)
# Place your actual secrets at: ~/.config/goose/secrets.yaml
# and keep GOOSE_DISABLE_KEYRING=1 if you want file-based storage.

GITHUB_COPILOT_TOKEN: null
# Add other keys as needed, e.g.:
# OPENAI_API_KEY: null
