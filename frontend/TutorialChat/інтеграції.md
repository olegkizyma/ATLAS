# Інтеграція системи обміну чатами з Атласом і Гусь

Цей документ містить рекомендації щодо реалізації функціоналу запитів і виведення відповідей у чаті між Гусь ({Goose}) і системою Атласу. Тут представлено можливі шляхи інтеграції для передачі та обробки повідомлень наступним чином:

---

## 1. **Розуміння поточних можливостей**
Перед розробкою необхідно оцінити архітектуру `/frontend`:
### 🔍 Основні аспекти для аналізу:
- **React-компоненти**:
  - Перевірити структуру React-компонентів для роботи з виводами.
  - Чи є існуючі таблиці для відображення відповідей чат-систем?
- **Контекст або API управління**:
  - Чи передбачено спільний контекст конфігурація (Context або hooks)?
  - Чи потрібна підтримка WebSockets для живого оновлення?
- **Реалізація запитів для REST (або GraphQL).**

---
## 2. Пропозиції щодо інтеграції із реальними запитами
### 🪝 **Використання Hook-компонентів в обробці завдань**:
#### Що потрібно:
- Новий React custom hook `useGooseIntegration`.
- Hook реєструє зміни:
```jsx
const { handleSendTask } => useGooseIntegration = () =>{

Setup-call, data Backend Integration```
---

## 2. Пропозиції щодо інтеграції із реальними запитами (Продовження)

### 🛠️ **API для обробки запитів і відповідей**:
На основі архітектури `/frontend`, рекомендую наступний підхід:
1. Використати WebSocket для активного зв’язку між сервером та компонентом чату.
2. Застосувати REST API для запитів нижчого пріоритету (повільної обробки).

### Приклад: Клієнтський код WebSocket
```tsx
import { useState, useEffect } from "react";

const useGooseIntegration = (task: string) => {
    const [response, setResponse] = useState('');

    useEffect(() => {
        const connect = new WebSocket("ws://<server-endpoint>");

        connect.onopen = () => {
            console.log("Connection established.");
            connect.send(JSON.stringify({ task }));
        };

        connect.onmessage = (event) => {
            const data = JSON.parse(event.data);
            setResponse(data.message);
        };

        connect.onclose = () => console.log("Connection closed.");
        return () => connect.close();
    }, [task]);

    return response;
};
export default useGooseIntegration;
```

### 🎨 Схема процесу взаємодії
#### Інтеграційна взаємодія через WebSocket:
```mermaid
graph LR
A[Компонент Чату] -->|Запит task| B(Сервер)
B -->|Обробка task на Server| C[Goose]
C -->|Відповідь
[[Message]]| B
B --> D[UI для Відповідей]
```

---

## 3. Рекомендації щодо Frontend:
1. **Контекст (Context API)**
   - Застосувати для глобального стану задач або відповідей.
```tsx
import { createContext, useContext } from "react";

const TaskContext = createContext({});

export const useTaskContext = () => useContext(TaskContext);
```
2. **Маршрутизатор (React Router)**.
   - Додати шляхи для сценарію тестування завдань і UI компонентів.
3. **Оптимізація pro-to-react:**
   - Частина компонентів WYSIWYG вивід відповіді через інтеграційні плагіни (Еxtra UI).
---

## 4. Інтеграція для звернення до Гуся через Chat Context:
### 📌 Реалізація логіки обробки запитів через Chat Context
#### Як працює:
- `chatContext` відповідає за буферизацію запитів до сервера Гусь і інтелектуальної обробки відповідей.
- Наприклад, чернетки повідомлень та динамічне оновлення повідомлень наступним чином.

#### Приклад відповіді: Оновлений ChatInput (React-компонент)
```tsx
import React, { useContext, useState } from "react";
import { ChatContext } from "../context/chat";

const ChatInput: React.FC = () => {
    const chatContext = useContext(ChatContext);
    const [message, setMessage] = useState("");

    const sendMessage = () => {
        if (chatContext && chatContext.sendTaskToGoose) {
            chatContext.sendTaskToGoose({ task: message });
        }
    };

    return (
        <div>
            <textarea
                value={message}
                placeholder={chatContext?.agentWaitingMessage || "Type your request to Goose..."}
                onChange={(e) => setMessage(e.target.value)}
            />
            <button onClick={sendMessage}>Send</button>
        </div>
    );
};
export default ChatInput;
```

### 💡 Роз’яснення:
- `sendTaskToGoose` – нова функція в ChatContext для надсилання завдання до Гуся.
- Введення повідомлення оновлює локальний стан, відправляє запит через логіку чату.
---

## 5. Стрімінговий Вивід Чату: Інтеграція з Goose

### 📌 Реалізація Покрокового Виводу Відповідей
#### Використання WebSocket для створення чату з потоковим виведенням:

### Код WebSocket-компонента:
```tsx
import React, { useState, useEffect, useRef } from "react";

const StreamingChat: React.FC = () => {
  const [message, setMessage] = useState(""); // Індивідуальний запит
  const [streamedOutput, setStreamedOutput] = useState(""); // Поточна відповідь у реальному часі
  const [isConnecting, setIsConnecting] = useState(false); // Стан з'єднання
  const wsRef = useRef<WebSocket | null>(null); // Референція до WebSocket

  useEffect(() => {
    // Підключення при завантаженні
    wsRef.current = new WebSocket("ws://<server-endpoint>");
    setIsConnecting(true);

    wsRef.current.onopen = () => {
      console.log("WebSocket connection established.");
      setIsConnecting(false);
    };

    // Слухати відповіді сервера
    wsRef.current.onmessage = (event) => {
      const data = JSON.parse(event.data);
      setStreamedOutput((prev) => prev + data.streamText); // Оновлює стрім
    };

    wsRef.current.onclose = () => {
      console.log("WebSocket closed.");
    };

    wsRef.current.onerror = (error) => {
      console.error("WebSocket error:", error);
    };

    return () => {
      if (wsRef.current) {
        wsRef.current.close(); // Очистка при завершенні
      }
    };
  }, []);

  const sendMessage = () => {
    if (wsRef.current && wsRef.current.readyState === WebSocket.OPEN) {
      wsRef.current.send(JSON.stringify({ message })); // Надсилаємо запит
      setMessage(""); // Очищуємо поле вводу
    }
  };

  return (
    <div>
      <h2>Streaming Chat with Goose</h2>
      <textarea
        value={message}
        onChange={(e) => setMessage(e.target.value)}
        placeholder="Type your message here..."
      />
      <button onClick={sendMessage} disabled={isConnecting || !message}>
        Send to Goose
      </button>
      <div>
        <h3>Streamed Output:</h3>
        <pre>{streamedOutput}</pre> {/* Потокове виведення */}
      </div>
    </div>
  );
};

export default StreamingChat;
```

### 💡 Переваги цього рішення:
1. **Реальний час**: Динамічне оновлення відповіді у чаті Goose без затримок.
2. **Покращення UI/UX**: Відображає відповіді Goose поступово, створюючи інтерактивний досвід для користувача.
3. **Масштабованість**: WebSocket-підхід дозволяє обробляти одночасні з’єднання без додаткових витрат.

---

### 📄 Рекомендації до Back-End Інтеграції (Goose):
1. **Створити WebSocket-сервер:**
   - Підтримка стрімінгового формату для великих відповідей (розбиття тексту на частини).
   - Відповідь сервера передбачає ключ `streamText`, що передає частини відповіді від Goose.
2. **Валідація запитів:**
   - Забезпечити обробку некоректних запитів.
3. **Підтримка відключення:**
   - Закриття з'єднання клієнтом має бути коректно оброблене.

---
---

## 6. Правки після детального аналізу існуючого фронтенду

### 🚀 Ключові місця для доопрацювання стрімінгового чату:

#### 1. `BaseChat.tsx` – базова логіка чату
- Переконатися, що функція `append` обробляє часткові потокові дані.
- Рекомендується додати валідацію нових повідомлень й інтегрувати цю логіку у функцію WebSocket (наприклад, `handleStreamedMessage`).

#### 2. `ChatInput.tsx` – введення та оновлення контексту
- Розширити існуючу функцію `handleSubmit` для покрокового надсилання частин запиту й обробки їх через WebSocket.
- Додати підтримку live-редагування у поточному контексті (`messages`).

#### 3. `ChatSettingsSection.tsx`
- Передбачити опції увімкнення/вимкнення потокового режиму для конкретного чату через `ModeSection`.
- Розширити опис `Scheduling Engine`, щоб охопити інтеграцію систем WebSocket.

### 📄 Додаткові рекомендації до файлу "інтеграцій":
1. Інтегрувати експериментальні функції ToolSelection і Response Styles для покращення взаємодії з Goose.
2. Створити у Readme документацію параметрів для стрімінгових WebSocket-запитів.
---

## 7. Додаткові рекомендації після аналітичного аналізу

### 1. `contexts/ChatContext.tsx`
- Розширити контекст, додавши підтримку стану стрімінгових повідомлень і керування прогресом.
- Використовувати функцію WebSocket або REST запитів для синхронізації контексту.

### 2. `ProgressiveMessageList.tsx`
- Інтегрувати прогресивне рендерування через `renderedCount` у логіку потокового виведення.
- Використовувати `isStreamingMessage` для відображення часткових даних у режимі реального часу.

### 3. `ToolCallWithResponse.tsx`
- Інтегрувати вивід логів інструментів у стрімінговий інтерфейс чату через компонент `ToolLogsView`.
- Передбачити обслуговування мультимедійних даних, таких як зображення чи ресурси, у відповідях Goose.

### 4. `PopularChatTopics.tsx`
- Інтегрувати механізм для автоматичної підстановки шаблонів запитів із “популярних тем” у поточний чат.
- Передбачити синхронізацію цього списку з сервером (додати WebSocket або SSE для оновлення тем у реальному часі).
